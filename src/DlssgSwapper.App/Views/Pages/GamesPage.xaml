<Page x:Class="DlssgSwapper.App.Views.Pages.GamesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28,18,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <StackPanel HorizontalAlignment="Left" MaxWidth="620">
                <TextBlock Text="Library" Style="{StaticResource PageTitleText}" />
                <TextBlock Style="{StaticResource PageSubtitleText}" Margin="0,3,0,0"
                           Text="Games managed by this tool. Select one to configure its proxy, then install or swap it." />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Spacing="8">
                <Button Command="{Binding AddGameCommand}" Style="{StaticResource ToolbarButton}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="14" />
                        <TextBlock Text="Add game" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding ScanSteamCommand}" Style="{StaticResource ToolbarButton}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE7FC;" FontSize="14" />
                        <TextBlock Text="From Steam" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <ListView x:Name="ProfilesList"
                      ItemsSource="{Binding Profiles}"
                      SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                      SelectionMode="Single"
                      DoubleTapped="OnRowDoubleTapped"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent"
                      BorderThickness="0">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,8" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardPanel}" Margin="0">
                            <Grid ColumnDefinitions="Auto,*,Auto">
                                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="10"
                                        Background="{StaticResource BrandSoftBrush}"
                                        VerticalAlignment="Center" Margin="0,0,14,0">
                                    <FontIcon Glyph="&#xE7FC;" FontSize="20" Foreground="{StaticResource BrandBrush}" />
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding ExecutablePath}" Style="{StaticResource CaptionText}"
                                               Margin="0,2,0,0" TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="4">
                                    <Border Style="{Binding InstallKind, Converter={StaticResource InstallStatePillStyle}}">
                                        <TextBlock Text="{Binding InstallState}" FontSize="12" />
                                    </Border>
                                    <Button Content="Configure" Style="{StaticResource ToolbarButton}"
                                            Command="{Binding ConfigureCommand}" />
                                    <Button Style="{StaticResource IconButton}" Command="{Binding RemoveCommand}"
                                            ToolTipService.ToolTip="Remove game"
                                            AutomationProperties.Name="Remove game">
                                        <FontIcon Glyph="&#xE74D;" FontSize="14" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="420"
                        Visibility="{Binding HasProfiles, Converter={StaticResource InverseBoolToVisibility}}">
                <Border Width="72" Height="72" CornerRadius="18" HorizontalAlignment="Center"
                        Background="{StaticResource BrandSoftBrush}">
                    <FontIcon Glyph="&#xE7FC;" FontSize="34" Foreground="{StaticResource BrandBrush}" />
                </Border>
                <TextBlock Text="No games yet" Style="{StaticResource SectionTitleText}"
                           HorizontalAlignment="Center" Margin="0,16,0,0" />
                <TextBlock Style="{StaticResource CaptionText}" TextAlignment="Center" Margin="0,6,0,18"
                           Text="Add the game's rendering executable, or pick it from your Steam library. Nothing is changed until you install." />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                    <Button Content="Add game" Command="{Binding AddGameCommand}" Style="{StaticResource PrimaryButton}" />
                    <Button Content="From Steam" Command="{Binding ScanSteamCommand}" Style="{StaticResource ToolbarButton}" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
